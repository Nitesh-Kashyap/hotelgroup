<table>
  <% @hotelgroups.each do |hotelgroup| %>
    <tr>
      <td><%= link_to hotelgroup.name, hotelgroup_path(hotelgroup) %></td>
      <td><%= button_to 'Edit', edit_hotelgroup_path(hotelgroup), method: 'get'%></td>
      <td><%= button_to 'Remove', hotelgroup_path(hotel), method: 'delete', data: { confirm: 'Are you sure?' } %></td>
  <% end %>
</table>
<%= link_to "Add a New Hotelgroup", new_hotelgroup_path %>